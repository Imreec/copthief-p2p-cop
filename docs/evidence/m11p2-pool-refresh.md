# M11 part 2 — real-opponent pool refresh (2026-08-13)

The gate for everything else in this milestone: every modeled rival arm resynced
to its source at a named sha, with its fidelity caveat stated, BEFORE any
self-play round trusts the pool. Standings table: `m11p2-pool-arena.md`
(generated by `config/arena_pool.json`, never hand-edited).

## Per-opponent disposition

| opponent | source verified | arm(s) | disposition |
|---|---|---|---|
| anrbj666 | shared repos @ cop `41e907d` / thief `f95b438` (pushed 2026-08-12) | `anrbj666-police`, `anrbj666-thief` (NEW) | REBUILT — their fielded brains, not the hunter instrument |
| nis-yar1 | counted logs (their `41b9fb76`, disclosed in-audit) | `nisyar1-thief` (REBUILT), `nisyar1-police` (NEW module) | counted play contradicted the friendly-modeled arms |
| uoh-sqak | public repo @ `72e7abc` | `sqak-evader` | STANDS — evader byte-identical to `1ca9d23`; only `counted_games_played` 0→2 moved since `d0bb80f` |
| vibecode | 08-10 logs (no new data) | `vibecode-police`, `vibecode-thief` | STAND — real-log-faithful, their stacks proven deterministic |
| best2934 | scout clone cop `9324fb50` / thief `c14c2c7b` (verified unchanged 2026-08-12) | `best2934-police`, `best2934-thief` | STAND |

## What the nis-yar1 counted diff found (arm-invalidating)

Modeled arms came from the PRE-counted friendly (their `a0ba98d1`); the counted
ran `41b9fb76` and both shapes changed:

- **Thief: the perch is dead.** Friendly g01 showed 21 consecutive STAYs at
  (0,4); counted g01/g03/g05 show 20-25% stays, longest run 4, away-moves
  dominant (18/28, 12/20), edge occupancy 40-54%. Rebuilt as a runner
  (flee-under-press + capped rests + border bias). Hint lie_rate 0.20-0.32.
- **Cop: six walls, not two — and the wall rule is exact.** All six placements
  per game (steps 9/18/21/27/30/34, byte-identical g02/g04/g06) land in
  DIAGONAL contact on the sorted-first of the two cells adjacent to both cop
  and prey. 27/33 moves closed on our lag-1 cell. anrbj666's independent mimic
  of this cop (their kill tapes) corroborates: approach to gap 2, hold, first
  wall step 9. New `nisyar1_cop.py` reproduces all six placements; the old
  vibecode-spec piggyback (max_walls 2) understated their wall pressure 3x.

The M11 champions beat both the stale arms and the live opponent (90-30 6-0),
so the staleness cost nothing this time — but a self-play loop tuned against
the stale cop would have optimized against one-third of the real wall budget.

## anrbj666 arm fidelity (studied at HEAD, ADR-0011 consent basis)

`anrbj666-police` models: BFS chase (STAY-incumbent ties), steady-heading
interception (flee-side projection, horizon 10), trap walls (range 3 / escapes
<= 3 / sharp >= 0.45) with their three wrinkles: step-in preferred mid-game,
rule-46 wall-on-peak in the last two turns, contact-dwell release at 4 sharp
contact turns. NOT modeled: their endgame solver and info-gain term (sharp feed
+ wall-on-peak carry conversion; info-gain is tie-shaping), their two x25
belief pins (approximated by `sharp199`).

`anrbj666-thief` models: blind key = lethal gate > min(openness,3) >
worst-wall (escapes, region) MIN over top-3 support > distance; trusted flip at
peak mass 0.75 to adjacency-avoiding wall-forecast play; fresh-flee flip back
to capped flight (believed-cop-distance proxy for their trail-freshness
signal). NOT modeled: the survival certificate (their own keep-gate: survival
identical with it off) and the stealth self-mirror tie-shaper.

## Standing warnings for anyone using this pool

1. **anrbj666 friendly tapes are poisoned by design.** Their friendly overlays
   field decoys: a de-fanged tail-chaser cop, a stall-metronome cop bait, a
   herdable distance-first thief, an NE-habit bait thief, and waypoint probe
   brains. Committed counted config fields the real brains. Model them from
   CODE at a named sha, never from friendly tapes. (Their bait explicitly
   targets tape-miners; their tests name nis-yar1 as the miner they counter.)
2. **They log our trail as evidence.** Both their roles law-solve our
   transmitted scent frames to a unique emitter per turn, pin it x25, and
   surface the track in their live report for audit diffing. Our honest frames
   localize us at lag 0-1 against them — relevant to the claim-threshold
   re-sweep and any concealment thinking, and it is exactly the mechanism the
   `truth-lag1`/`sharp199` feeds approximate arena-side.
3. **League motion noted while verifying sources:** uoh-sqak banked a counted
   vs vibecode 2026-08-12 ("F14 complete, 2 of 2"); anrbj666 lost a friendly
   1-5 (45-85) to nis-yar1 on 08-11 17:15 — nis-yar1 iterates fast and beat
   the pool's strongest studied cop the same day we swept them. Their counted
   shape here is one commit older than whatever they run next.
