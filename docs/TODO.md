# TODO — Cops-and-Robbers P2P Race

> **Status: APPROVED (Phase 2 gate, 2026-07-16).** **This copy: the police (cop) repo.**
> Living document: checkboxes tick as work
> **lands on `main`**, never as aspiration. Statuses: ☐ not started · ◐ in progress · ☑ done.
> Owner key: **I** = Imree (drives, approves, operates sends/arming) · **C** = Claude (authors
> code/docs under I's direction) · **E** = Eyal (reviews, per-task ownership) · **AG** =
> Antigravity (cross-model PR review). Every task's DoD includes: CI green, ≤150-line files,
> tests per TDD, reviewed PR. ⚑ = role-repo-specific task.

## Phase M0 — Process bedrock *(gate: PRD/PLAN/TODO approved in repo form)*

- ☑ **M0-1** Scaffold both repos (uv, pyproject, ruff/mypy/coverage config, empty `src/` layout per PLAN §3) — I+C. DoD: CI green on empty src in both.
- ☑ **M0-2** Port + adapt CI workflows from HW6 (keyless gates: ruff, mypy --strict, pytest+coverage fail_under, 150-line check, no-hardcoded scan, secret scan) — C, review E. DoD: each gate demonstrably fails on a seeded violation. (M0 record: anti-pattern + mirror gates fired on real violations; remaining gates' negative tests ride with the M1 suite.)
- ☑ **M0-3** `scripts/sync_core.py` + `sync_manifest.json` + CI manifest gate — C, review AG. DoD: PLAN §13 M0 drift test observed (induced drift → sibling CI red).
- ☑ **M0-4** CLAUDE.md ×2 (HW6-derived, rubric-V3 diff applied, kit-conformance clause) — C, approve I. DoD: I approved both files.
- ☑ **M0-5** Port `.claude/skills/` (commit-discipline, pr-discipline, tdd-cycle, self-grade, eval-harness→arena-harness rewording) + `docs/REVIEW_PROCESS.md` — C. DoD: skills reference this project, not HW6.
- ☑ **M0-6** Land approved PRD/PLAN/TODO as `docs/` in both repos (⚑ role deltas applied) — C, approve I. DoD: docs match approved drafts + amendments.
- ☑ **M0-7** `docs/adr/0001-sync-mirror.md` + `0002-reference-reuse.md` — C. DoD: ADR format per PLAN §14.
- ☑ **M0-8** `scripts/check_submission.py` skeleton (App C table 6 + guidelines §17 checklist, red until items land) — C. DoD: runs in CI as non-blocking report.

## Phase M1 — Walking skeleton *(PRD_engine + PRD_crypto precede code)*

- ☑ **M1-1** `docs/PRD_engine.md` + `docs/PRD_crypto.md` (mechanism PRDs) — C, approve I.
- ☑ **M1-2** `domain/board+rules+scoring` (config-driven, App F guard in `shared/config`) — C, review E/AG. DoD: unit tests incl. capture-by-barrier, imprisonment, tie rule; ≥90% cov.
- ☑ **M1-3** `domain/crypto` from kit CORE (canonical, commit, terms sig, game_uid) + kit vectors as CI fixtures — C. DoD: all CORE vectors green in both repos.
- ☑ **M1-4** `domain/state machine` (transition table per PLAN §5) — C. DoD: property tests reject all illegal transitions.
- ☑ **M1-5** `wire/` dataclasses + validation (mirror reference fields; forward-compat rule) — C. DoD: schema tests incl. reject-missing/tolerate-unknown.
- ☑ **M1-6** `infra/mcp` server (4 tools) + client + in-process fake; `peer/` minimal loop (handshake→turns→audit, geometric play, template hints stubbed) — C, review AG. DoD: PLAN §13 M1 — one command, full localhost mini-game, self-audit pass. *(Observed 2026-07-17: `uv run copthief run p2p-match` — two processes, real FastMCP, mutual audit Verified OK; evidence in `docs/evidence/m1-p2p-match.md`.)*
- ☑ **M1-7** `sdk/` facade + CLI entry (`run peer`, `run local-match`) — C. DoD: all M1 flows callable only via sdk.
- ☑ **M1-8** JSONL logger (verbatim bytes, transitions, decisions, provenance) — C. DoD: M1 game replayable from log.

## Phase M2 — 🚦 Oracle spike (go/no-go gate)

- ☑ **M2-1** Reference peer run as oracle (sha 960499fd, keyless); Cloudflare named tunnel `copthief` on `imreeyal.com`, both hostnames (OI-3 decided, ADR-0006) — I+C. DoD observed 2026-07-18: reachability both directions through the public edge. *(Stage A ran both peers on one machine behind the tunnel — the second-machine environment arrives with the Stage-B VPS, OI-4.)*
- ☑ **M2-2** Both role pairings vs the live reference over public URLs: negotiate locks terms → play → mutual audit Verified OK both directions; shared game_uid property verified cross-implementation — I+C. DoD: spike notes §6 + four JSONL logs in `docs/evidence/`.
- ☑ **M2-3** SQ1/SQ2/SQ3 answered in writing against the running reference (spike notes §3, live-confirmed §6; SQ3 feeds the M3 belief/audit PRDs) — C.
- ☑ **M2-4** `docs/adr/0003-crypto-early.md` + `0006-deploy-tunnel.md` (spike results) — C.
- ☑ **M2-5** **GO/NO-GO review with I** — **GO, 2026-07-18** (Imree, on the Stage A evidence + Claude's candid assessment; residual gaps F9/Stage-B/scent-live/series documented in spike notes §8 and scoped to M3/M6/M7) — I.

## Phase M3 — Perception + arena

- ☑ **M3-1** `docs/PRD_scent.md` + `docs/PRD_belief.md` (SQ1-informed) — C, approved I (PR #16, 2026-07-18).
- ☑ **M3-2** `domain/scent` (kit-pinned form; locked-model doc w/ numeric example; ADR-0004) — C. DoD: kit pheromone vectors + decay/emission tests green.
- ☑ **M3-3** `domain/belief` exact Bayes filter (motion×scent×hint) — C, review E. DoD: beats last-known-position tracker on belief-error in referee sims (PLAN §13 M3).
- ☑ **M3-4** Gazetteer + hint templates (map_area landmarks; ≤hint_max_words; intent flag wired to sealing) — C. DoD: our hints round-trip our parser; injection-safety tests (hostile hint corpus).
- ☑ **M3-5** Baseline brains (random, greedy-Manhattan) via BrainBase seam — C. DoD: full headless series in referee + peer modes.
- ☑ **M3-6** Arena harness (sdk consumer; seeded round-robin; tables; champion regression gate in CI) — C, review E. DoD: seeded reproducibility test; gate red on champion loss.
- ☑ **M3-7** 🚦 **ADR-0004 revision decision — DECIDED: REVISE** (I, 2026-07-18, on C's seam assessment; league coordination `notes/LEAGUE-COORDINATION-ALON.md`). Add a second named scent model `multiplicative_book_v1` beside `subtractive_chebyshev_v1`, pair-locked via the locked-model doc + refusal rule, belief observation model parameterized. Assessment record: the cadence seam is two isolated decay calls in `peer/turns`; per-field decay count is once per FULL turn in reference and ours alike (the "half-turn" framing was imprecise — the real deltas are anchor/order/receiver-side convention); the locked-model doc is hashed+logged but NOT terms-signed (can't be, reference-frozen key set) — the build adds step-0 sealing of the model hash to close that.
- ☑ **M3-8** Named scent models — **BUILT** (gate PR #56 = approval; built in ADR-0004 v2's order). `domain/scent_models` + `domain/scent_book`: one protocol, three primitives (`deposit`/`decay`/`advance`), `advance` being the cadence primitive that owns each model's ORDER. `subtractive_chebyshev_v1` stays the default and stays byte-identical in game bytes (kit pheromone vectors + every referee/arena pin pass untouched). `config/locked_models.json` carries the six kit registrations VERBATIM — all six hash to the kit pins under our canonical form, including anrbj666's re-emitted `934c220d…`. Declaration moved to the kit SPEC §7 envelope (**`scent_model_sha256` only; the doc no longer crosses the wire** — a deliberate negotiate-bytes change, PRD §9.5 amended); refusal only when BOTH declare and differ (five-row truth table as permanent conformance CI); model hash SEALED at step 0 (closes v1's hashed-but-not-commit-bound gap); belief observation model reads its falloff from the selected model (linear vs logarithmic age inversion); M6-7 physics check compares with TOLERANCE for a model that does not round, byte-wise otherwise. `assert_agrees_with` refuses a registration that contradicts the signed constitution. **Per-model belief eval RERUN and committed — and it is a real finding: the book model costs most of the filter's edge** (mean error 0.9001 vs 0.7314, hit-rate 17% vs 98%), from additive-kernel saturation (12/49 cells pinned at 0.9 by turn 4 — inherent) plus our age-inversion heuristic (fresh ring-2 reads age 14 — improvable). **Open for the Alon call:** the registration's `transmitted: false` means a peer honouring it puts NO grid on the wire, so under `reference-v3` wire the scent channel would carry nothing and the M6-7 check would have nothing to check.

## Phase M4 — Observability

- ☑ **M4-1** `docs/PRD_gui_replay.md` — C, approved I (PR #24; merge = the approval).
- ☑ **M4-2** Live GUI (heatmap + turn banner; local truth only) — C (PR #26 + workstream-L log schema PR #25). DoD observed: `assets/m4-live-heatmap.png` from the real audited game `m4-local-g1` (evidence: `docs/evidence/m4-observability.md`).
- ☑ **M4-3** Replay verifier (Verified OK / TAMPERED) — C, review AG (PR #27). DoD observed: M2 g1 + M3 logs → Verified OK; mutated copy → TAMPERED exit 1 (both quoted in `m4-observability.md`; rule-19 mutation matrix pinned in CI; viewer screenshots in assets/).
- ☑ **M4-4** Belief-vs-truth overlay + belief-error curve export — C (PR #28). DoD observed: `assets/m4-belief-overlay.png` + `-curve.png` rendered from the real audited game via `copthief overlay`.

## Phase M5 — Intelligence ⚑

- ☑ **M5-1** ⚑ `docs/PRD_police_brain.md` (police repo) / `docs/PRD_thief_brain.md` (thief repo) — C, approved I (police PR #30 + thief PR #20, approved together; merge = the approval; ADR-0005 rides the police PR).
- ☑ **M5-2** ⚑ PoliceBrain: expectimax over belief + barrier graph-surgery + capture-commit (claim policy per SQ2) — C, review E/AG (PRs #31 core seam + #32 role package). DoD OBSERVED, CI-blocking: 24/32 = **75%** vs `ref-thief` over the 32-scenario suite (floor 60%; `docs/evidence/m5-arena.md`); champion pin dethroned twice with same-PR evidence (ref-police → police-brain).
- ☑ **M5-3** ⚑ ThiefBrain: region-survival + articulation awareness + deception timing — C, review E. **Delivered SIBLING-SIDE (thief repo PRs #21+#22); this copy tracked it as not-started until 2026-07-20 — a doc↔repo gap found while auditing open items, corrected here.** DoD OBSERVED there, CI-blocking: 25/32 = **78%** survival vs `ref-police` over the 32-scenario suite (floor 60%; holdout seeds 101–132: 72%). Candid note recorded in the thief TODO: the greedy baseline ties 25/32 on that suite, so the pin honestly stays greedy — the trap machinery targets barrier-surgery cops and is unmeasurable cross-repo. Role brains never mirror (⚑), so nothing of this lands in this repo's tree.
- ☑ **M5-4** Genetic tuning runs (HW6 salvage adapted; weights → config) — C (PR #35). DoD OBSERVED: improving fitness curve committed (`docs/evidence/m5-ga.md`, 0.938→1.000 best, elitist-monotone by construction; `config/ga_weights.json` artifact). Evolved police weights validated OFF-suite (DoD 75%→**94%**, holdout 78%→97%) and deployed as config; smoke GA = permanent keyless CI. ⚑ thief-side run rides the next sync (its `ga.json` parity + ThiefBrain gene box).
- ☑ **M5-5** Post-audit opponent profiling (lie-rate, motion priors → next mini-game) — C (PR #38). DoD OBSERVED: PLAN §13 M5 prior-shift test in CI (`tests/integration/test_prior_shift.py` — a lying thief's verified audit shifts mini-game 2's hint trust below the default, never below `[belief] profile_hint_floor`); `strategy/profiling.py` (lie-rate + motion prior from revealed intent/move labels; scent honesty NOT profilable — grids never sealed, SQ3) + `PeerSession(hint_trust=…)` seam + `profile` JSONL event on every verified audit. Belief math untouched (M3-8 boundary).
- ☑ **M5-6** Template-bank A/B in arena (deception efficacy metric) — C (thief-led; core instrument = cop PR #39: named banks + referee verbal seam + truth-anchored efficacy trace + `deception_ab` runner; `[strategy] hint_bank` ships the winner). DoD OBSERVED in the thief repo (measured table `docs/evidence/m5-template-ab.md` + notebook render; winner `classic` shipped): banks identical vs our closed-vocab parser (wording-neutral by construction); lie efficacy real but tiny (+0.000042/lie — the timing policy fires when the exact-Bayes tracker is already near-certain; disclosed).
- ☑ **M5-7** `notebooks/results_analysis.ipynb` (arena + sensitivity + curves; LaTeX + citations) — C, review E (PR #40). DoD OBSERVED: executed notebook committed WITH outputs (arena standings + champion gate GREEN · DoD 30/32 = 93.8% PASS · GA curve 0.688→1.000 · w_distance/p_commit sensitivity sweeps as rendered PNGs); renders-clean pin permanent CI (`tests/integration/test_notebook.py` — execution counts, zero error outputs, figures present). ⚑ thief repo commits its own notebook (adds the M5-6 A/B table render).

## Phase M6 — Reporting & fairness rail

- ☑ **M6-1** `docs/PRD_reporting.md` + `docs/PRD_gatekeeper.md` — C, approved I (PR #43; merge = the approval; D-decisions ruled on the PR thread).
- ☑ **M6-2** Four artifact schemas + writers (game_uid naming; canonical bytes = emailed bytes) — C, review AG (PR #44). DoD OBSERVED: byte-level conformance battery vs reference `docs/sample-run/` = permanent CI (every embedded hash recomputes through our code; all four files reproduce byte-for-byte from our serializer; conformance find: the reference locks `schema_version`+`_note` UNDER `config_sha256`). ⚠ Consensus signature (settlement-critical; credit Alon, verified vs reference `report_writer.py` @960499fd lines 22-25/81): SHA256 over `json.dumps(data, sort_keys=True, ensure_ascii=False)` with **DEFAULT (spaced) separators** — a third canonical variant, NOT our compact form — computed over the report **BEFORE** the `חתימת_קונסנזוס_משותפת` key is inserted (sign-then-insert); report schema is Hebrew-keyed per book §8.
- ☑ **M6-3** Step-0 declaration builder (hardware, model, tokens, commit hash, game-count) + signing — C (PR #45, stacked on #44). DoD OBSERVED: `test_current_commit_hash_is_the_real_repo_head` + the step-0 record in every audit; replay re-hashes unpaired records (rule-19 gap closed) and the mutation matrix covers the token fields + a forged declaration. Per-step token counts (`tokens_step`/`tokens_total`) go **INSIDE the sealed record** (`peer/sealing`) — the reference's own sealed schema does this (SQ3 list; league coordination re-confirmed).
- ☑ **M6-4** Gmail sender (compose-scope OAuth per D1=A; HW6 flow) + **draft default + arming interlock** — C, operate I (PR #47: code complete — interlock truth table proves exactly one combination sends; emailed bytes == artifact file bytes; gatekept). DoD OBSERVED live 2026-07-20 at `d819802` (OI-5 done: dedicated team account `imreeyal.copthief@gmail.com`, GCP project `copthief-league`, Desktop client, `gmail_auth.py` consent): one local series' result artifact landed **as a Gmail draft** in the team mailbox through the full rail — evidence `docs/evidence/m6-email.md` + `assets/m6-email-draft.png`, body byte-identical to the artifact (2814 B), authorized mailbox confirmed by live `getProfile`, credentials git-ignore-verified before use. ⚠ The **draft-first operating posture is superseded for counted play** — see M7-6 (App E rules 30/32/34/35 + book §9.3 found during this run); the draft rail itself remains correct for dev/CI/friendly use.
- ☑ **M6-5** Gatekeeper (quota → token bucket → DoS lock; `rate_limits.json` ≥ signed minimums asserted) — C (PR #46). DoD OBSERVED: synthetic-load test queues and never crashes (12 threads, fake clock); 429 backoff schedule pinned (`retry_backoff_sec × attempt`); breaker trip/cooldown/reset in CI; v1.01 tighten-only service band asserted.
- ☑ **M6-6** Series runner (`num_games`, per-game config commit, roles) — C (PR #48). DoD OBSERVED in CI: 2-game local series → all artifacts validate (declaration + per-game configs/logs + result + Hebrew reports), roles alternate (F2), sealed game-count truthful, per-game logs replay Verified OK, M5-5 trust carry wired, email resting state refuses end-to-end. Live alternation-vs-reference stays flagged for the next authorized friendly (M2 §8 gap 4).
- ☑ **M6-7** Chaos drill harness (PLAN §10 drills) **+ watchdog/persistence (FR-8) + the FR-11 scent-physics audit check (D2-approved rider, PRD_gatekeeper §5 — this amendment)** — C, review E (PR #50, stacked). DoD OBSERVED: every drill's SPECIFIC defense asserted in permanent keyless CI (10-drill battery: comm/inbound/audit/watchdog); evidence committed `docs/evidence/m6-chaos.md`; watchdog armed on every live peer (heartbeat seam, signed timeout, snapshot + loud exit); scent-physics = evidence-grade event only, verdict untouched (SQ3). Residual: the real-tunnel kill variant is operator-run, rides the next live session.
- ☑ **M6-8** COST.md + token accounting on every LLM path — C (PR #49, stacked on #48). DoD OBSERVED: `COST.md` double-proof ledger + permanent CI pin `tests/integration/test_zero_tokens.py` (every sealed game record charges 0/0 on a real audited game AND the log replays Verified OK — the claim is tamper-evident, not just logged).

## Phase M7 — League ops *(nothing announced unless true of the tree)*

- ☐ **M7-0** 🚦 **Wire-shape mutual ADR (joint with Alon/Renat) — PRE-SERIES BLOCKER for any counted game vs their team** — C drafts, I approves; co-signed by both teams; nothing posts/sends/co-signs without I's word. Position (citations book-verified 2026-07-19): `reference-v3` **resolves** the book's Ω_i self-contradiction (ch.1 §1.3: "no agent sees its rival" — rival position formally excluded from the observation space; ch.5's Reveal **deferred to the audit boundary, not skipped**); App D §4 precedence ("where the repo deviates, the book prevails") demotes demo interop to **corroboration** — **now corroboration INDEPENDENTLY VERIFIED BY THE COUNTERPARTY (2026-07-20):** the Alon/Renat team re-verified the staged full-pairing game (`deee14f6…`, package `notes/outbox-alon-verification/`) with their own tooling and reported **every claim green** — 35 commit-reveals, byte-identical `smell_grid`s under their from-scratch `subtractive_chebyshev_v1`, barrier growth 0→7, standard constitution, and they re-derived our `game_uid` from our committed config themselves; physics recomputed under `reference-v3` per our caveat. The ADR may now cite the demo games as corroboration *verified by the other side*, which is materially stronger than "our own logs"; `bookletter-v3` = registered **documented deviation** a pair locks by explicit sign-off (kit issue #6 disposition: register both shapes, asymmetric status); the wire-shape lock rides the SAME handshake locked-model doc schema as the scent models (M3-8 prerequisite, pinned once). Balance evidence committed: `docs/evidence/wire-shape-balance.md` (PR #41) — **common knowledge favors the EVADER** (aggregate cop 45.8%→26.6%; police-brain vs evading thieves 31/32 & 30/32 → 0/32), so "who adapts" is a competitive term, not a convenience. Drafts staged: `notes/DRAFT-ADR-WIRE-SHAPE.md`, `notes/DRAFT-REPLY-ALON-R2.md`, `notes/DRAFT-ISSUE6-DISPOSITION.md`.
- ◐ **M7-1** Sparring host deployed (both roles, generic brain; OI-4) — I+C. DoD: external reachability check passes 24h. IF the M3-7 ADR-0004 revision is approved: sparring peers accept either named scent model at negotiation. **SAFETY HALF BUILT 2026-07-22, ahead of the host** — pulled forward because the Alon/Renat team asked for a peer window within hours (round 8) and a hurried config edit is exactly where the standing rules get broken. `shared/sparring` turns two remembered rules into checked properties of a loaded config: **no tuned strategy weights** (CLAUDE.md §9/§10 — a sparring peer answering with our tuned brain hands a future counted opponent a free sample of the graded thing) and **no mail** (ADR-0008 decision 6, whose 'asserted at startup' correction is now DISCHARGED — `enabled=false` AND no recipient, because the recipient is the authorization). Enforced at the moment of use by `copthief run peer --sparring`, which answers a wrong config with a JSON refusal + exit 2 rather than a traceback. `scripts/make_sparring_config.py` derives a host config from the committed one: constitution/limits/locked-models copied BYTE-FOR-BYTE (they are hashed into `config_sha256` and the terms signature — a host negotiating different terms is not our peer), every `[strategy.<role>]` table dropped so each brain falls back to its canonical DEFAULT_OPTIONS, `[email]` forced to rest, and the tuning artifacts (`ga*.json`, `arena*.json`, `balance.json`, `self_grade.json`) LEFT BEHIND rather than merely unread — a directory holding them is one `--config` away from being played. **The script validates its own output** through the same guard, which is not ceremony: a tuned table written INLINE under `[strategy]` is invisible to the text transform and caught only by loading the result (pinned as a test). Output is git-ignored — a second copy of the constitution is a drift hazard; the recipe carries the guarantee. 14 new tests, role-agnostic per gotcha #9. **STILL OPEN for ☑: the deployment itself** (OI-4 — tunnel-on-demand vs Stage-B VPS, `notes/DRAFT-ACCEPTANCE-TARGET-OPS.md`) and the 24h reachability DoD.
- ☐ **M7-2** Kit: wire-contract page + JSON-schema fixtures + audit fixture (incl. tampered record) + **pre-match checklist & netcheck script** + SPEC §4 `_g<NN>` naming clarification — C, review E. DoD: kit CI green; I merges. League-coordination additions, each **"verify present, else add"** (an earlier kit PR from the planning session may land some first): `report_consensus_signature` vector (spaced form + sign-then-insert; **credit Alon by name**) · F-421 Host-header deployment note (posted as kit issue #4) · named scent-model vectors (M3-7-gated) · **locked-model DOC-SCHEMA pin** (one schema serving scent models AND wire shapes; M3-8/M7-0 prerequisite) · named wire-shape registrations per the issue-#6 disposition (M7-0-gated; `bookletter-v3` fixtures = Alon's team's contribution, credited, PROPOSED until independently reproduced).
- ☑ **M7-3** League outreach round (Alon first) + demo friendlies; cross-audit clean — I (all sends I-authorized). DoD OBSERVED 2026-07-25: **the first external friendly PLAYED AND AUTO-REPORTED** — imreeyal vs anrbj666, six sub-games, one wire-locked `game_uid`, every mutual audit clean, all six logs replay **Verified OK** (368 records total), winner imreeyal 75–35, ONE report auto-fired to both teams (no human between the sixth settlement and the send), opponent's own report agrees field-by-field on every game value. Evidence: `docs/evidence/m7-3-first-external-friendly.md` + `m7-3-friendly-g01..06.jsonl` + the committed artifact set `reports/imreeyal/`. Two evidence-grade discrepancies raised with the opponent, both since resolved (2026-07-26): their `game_uid` was deterministically derived from the WRONG INPUT (their whole `game.json` vs the reference's flat negotiated terms — diagnosis corrected by them, fixed and verified against our value to the digit), and the `game_id` order is now derived-sorted on both sides (reference `derive_game_ids`; our M7-17). Residual mutual finding: uid divergence is silent during play (never crosses the wire) — declare-at-negotiate proposed as the closure. The seven-window campaign ledger and the hardening it produced are in the evidence doc.
- ☐ **M7-4** First counted series (authorized by I; the M7-6 per-run path) — I. DoD: both reports accepted; artifacts + config committed; profiling notes stored.
- ☐ **M7-5** Additional counted series vs distinct teams as roster allows (target: all reachable) — I. DoD: per-series artifacts committed.
- ☑ **M7-6** 🚦 **Email posture correction — PRE-SERIES BLOCKER for any counted game** — C builds, I approves (amends a hard constraint, so PRD/ADR gate first). Found 2026-07-20 during the M6-4 live run (`docs/evidence/m6-email.md` §Disclosures): the book **requires automatic reporting** — App E **rule 32** (sanction: absence of reporting voids that game's points), **rule 35** (one team's non-report disqualifies the game for **BOTH** teams, score 0), **§9.3** *"אין עוד מקום להתערבות אנושית"* + the lecturer address as *"הכתובת היחידה והמחייבת"* (`rmisegal+uoh26finalgame@gmail.com`). Our per-send arming interlock therefore cannot be the counted-series posture: it risks our points **and Alon's team's**. **Book-internal contradiction to document (academic-freedom clause):** App E rule 30 + App A §1.3/§3 mandate `gmail.send`-only (sanction: security deviation → code disqualification), yet App B's own `game.toml` listing ships `mode = "draft"` and p.139 describes the reference report as *"נשלח כטיוטת Gmail"* — and `gmail.send` **cannot** create drafts. **GATE DOCS WRITTEN 2026-07-20, awaiting I's approval — `docs/adr/0008-email-posture.md` + `docs/PRD_reporting.md` §5a; no M7-6 code until both are approved.** Decisions taken there (I's rulings this session): **automatic send is the posture for every real run**; **authorization = the configured RECIPIENT, no arming flag** (a boolean says "sending is allowed", the recipient says *who* — and nobody types the lecturer's address by accident); **draft is DROPPED as a mode and the live token becomes `gmail.send` only** — reviewing the bytes is done by sending **to ourselves**, which exercises more of the path, and dropping draft removes the need for `gmail.compose`, resolving rule 30 **literally instead of by argument** (CLAUDE.md §4 reverts to send-only, citing rule 30 + App A; the draft code stays, tested, unreachable on a send-only token); `recipient` becomes a **list** (friendly = us + opponent; counted = lecturer **only** — a peer cross-check is its own run, never a CC); the artifact is **attached as a JSON file** (rule 34) beside the byte-identical body; the **sparring host stays hard-pinned to non-sending** (PLAN §2, startup assert). **Amends CLAUDE.md constraint #16 in BOTH repos + the parent-workspace standing rule**: per-send word → "no email is ever sent to an address I have not configured for that run" (authorization before the match, never inside it). **Sequencing is the risk control:** the lecturer is addressed only after self- and peer-friendlies prove the format on both sides (rule 35 punishes contradictory reports as harshly as missing ones). Runaway protection stays the **gatekeeper/token bucket** (rule 28, M6-5) — the book's own answer to its §9.3 "thousands of messages a minute" scenario. Primary-source checks recorded in the ADR: `gmail.send` is NOT among `users.drafts.create`'s scopes (so the book's own `mode="draft"` listing is impossible under its rule 30), and `users.messages.send` carries recipients as RFC-822 headers (so multi-recipient friendlies work send-only). **BUILT 2026-07-20 (PR #54): code + tests + config + CLAUDE.md/PRD/PLAN wording all landed** — interlock is now (enabled × mode × recipients) with every no-recipient combination refusing; `gmail.send` scope; multipart body + attached JSON artifact (rule 34); `recipient` a list; resting state `enabled=false` + `recipient=[]`; `EmailSettings.recipient: tuple[str, ...]`; email sender tests split (fixtures extracted to `tests/unit/infra/email_fixtures.py`, refusals to `test_email_refusals.py`) for the 150-line rule. **REMAINING for ☑: (a) Imree re-runs `scripts/gmail_auth.py` for a SEND-ONLY token** (~5 min; the compose token still on disk cannot be used by the shipped scope), **(b) the thief sync ritual** (mirrored core + its own CLAUDE.md #16/§4 amendment), **(c) the parent-workspace standing rule** in `FinalProject\CLAUDE.md`. Deferred by design: the sparring-host `enabled=false` startup assertion lands with the host itself at M7-1 (ADR-0008 decision 6, corrected — no sparring runner exists yet). **CLOSED 2026-07-20:** send-only consent run and verified (token holds `gmail.send` alone - `getProfile` now 403s, i.e. it cannot even read its own profile); thief sync merged (thief PR #35, main `9ab4dce`); parent-workspace standing rule amended to "no email is ever sent to the LECTURER without Imree's explicit word" (outreach mail keeps the original per-send rule). **Plus the lecturer guard (PR #55, ADR-0008 amendment 9b):** Imree caught that his standing rule was still only policy - the interlock refused a run with NO recipient but did not know which address was the lecturer's. Now `[email] lecturer` is config-owned and the interlock REFUSES it whenever `counted` is false (case/whitespace-insensitive, including hidden in a list) - reusing the App F `counted` switch, which cannot be set by accident because a counted constitution will not load unless it is a genuine six-mini-game match. Friendlies pay no ceremony.
- ☑ **M7-7** 🚦 **Live-path defects from the real-tunnel kill drill — (1) was a PRE-SERIES BLOCKER, now CLOSED** — C, review AG. **All four fixed 2026-07-20** (TDD RED→GREEN, keyless CI; evidence `docs/evidence/m6-chaos.md` §M7-7). **(1)** The heartbeat measures **loop liveness, not I/O duration**: `peer/watchdog_transport.WatchedTransport` declares all four blocking `PeerTransport` calls as I/O windows, and inside a window the watchdog measures against a **derived** I/O budget (`turn_timeout_seconds + watchdog_timeout_sec` = 240) — so the **signed `watchdog_timeout_sec` is untouched and the App F guard intact; the fix is semantics, not a config bump**. `shared/budgets.reconcile_budgets` asserts the four-rule ordering **at config load** (the part that was missing entirely — the budgets had never been related to each other anywhere and met for the first time at runtime, in a live game). DoD OBSERVED in permanent keyless CI (`tests/chaos/test_watchdog_io_drill.py`): a scripted blocking transport stalls an outbound push for 3× the loop budget on a real `run_peer_game` and the watchdog **never fires** (game finishes), while a genuinely wedged loop **still** persists + shuts down, and an I/O wait past even the I/O budget still fires — a backstop, never an off switch. **(2)** Snapshot pinned to the git-ignored `logs/` via role-derived `sdk/peer_run.snapshot_path`; `state_*.json` git-ignored repo-wide as well. **(3)** `peer/watchdog.announce_stall` writes **and flushes** before `os._exit(1)`. **(4)** Replay reads `peer_result`, with `RESULT_EVENTS` order as **precedence not preference** (the M1 pin caught it immediately: a local log holds both events and a per-side payload counts only its own steps — 4 where the match played 5); `game_uid` falls back to the `negotiated` event; rider closed — `records_verified` is on the summary and the CLI banner, and a verdict over **zero** records is now **TAMPERED**. M5 friendly re-replayed, banner populated (13 steps, `cop_capture`, 28 records). Aborted-game→TAMPERED kept as-is (fail-closed, disclosed). **LIVE RE-DRILL RUN AND CLEAN 2026-07-20** (`docs/evidence/m7-7-redrill-g1.jsonl`; same tunnel rig, `cloudflared` killed at the handshake so the edge stayed dead all game): **zero `watchdog_stall` through 178 s** — where `a23d7ce` fired at ~60 s — and at 183 s the process exited on **its own turn budget**, logging `transition waiting_for_opponent → technical_loss, trigger "turn deadline exhausted"` with `outcome: timeout`. **Lost by rule, not by self-termination** — the blocker, closed in the field. Defect (2) verified in the same run: no snapshot written, `git status` clean. **RESIDUAL NOW CLOSED (2026-07-21, Imree approved, PR #60):** the outbound-push half — an in-game `send_turn` that exhausts its retry budget — was raising a naked `TransportError` rather than classifying. Scoped to Imree's ruling (in-game pushes only · transport-exhaustion only · no unilateral outcome claims · audit path verified): `TransportError` moved to the protocol seam (`peer/transport`) so `run_peer_game` classifies transport-blind; the in-game push retries on the **turn** budget (`McpTransport.turn_push_timeout`, handshake+audit keep the connect budget) so a mid-push flap tolerates as long as a silent-opponent flap; an exhausted push becomes **our own** `timeout`→`TECHNICAL_LOSS` (trigger `outbound turn undeliverable past the turn budget`, distinct from the inbound `turn deadline exhausted`), settling down the identical audit-skipped path — no unilateral claim, and a non-transport error still propagates. Permanent CI: `tests/integration/test_push_exhaustion.py` + `tests/unit/infra/test_transport_budgets.py`. Practical tolerance now 180 s in both directions. **LIVE MID-PUSH DRILLS RUN AND CLEAN (2026-07-21, Imree authorized; `docs/evidence/m6-chaos.md` §Live mid-push tunnel drills + `m7-7-push-heal-g1.jsonl` / `m7-7-push-exhaust-g1.jsonl`):** our cop vs the reference thief over the real tunnel, kill front-running our reply push. **(a) heal-within-budget** — edge dead 90 s then restarted: the retrying push **delivered**, the game resumed and finished `cop_capture` in 13 steps (mutual audit OK, replay **Verified OK**), zero `watchdog_stall`, no classification, exit 0. **(b) budget-exhausted** — edge dead past 180 s: `transport_error` (opponent unreachable, `530`) → `transition awaiting_reveal → technical_loss, trigger "outbound turn undeliverable past the turn budget"`, `outcome: timeout`, `opponent_claim: ""`, **exit 0** (no raw `TransportError`), no snapshot, `git status` clean, report rail `refuse: email disabled` per posture; the aborted log replays **TAMPERED (exit 1)**, fail-closed. Gotcha #11 both directions (reference configs already on the tunnel, left untouched, cloudflared stopped after). **M7-7 is now fully closed — both live observations are in the tree.** Original findings, all four observed 2026-07-20 at `a23d7ce`, evidence `docs/evidence/m6-chaos.md` §Real-tunnel kill drill (+ `m6-tunnel-kill-g1.jsonl`, `m6-tunnel-g1-capture.jsonl`). **(1) 🚦 The watchdog outruns our own turn deadline.** A blocking outbound call stops the heartbeat, so the signed `watchdog_timeout_sec` (60) fires before the private `turn_timeout_seconds` (180) can classify a silent opponent — a >60 s network flap makes us lose a counted game by self-termination instead of by rule. Fix: the heartbeat must measure **loop liveness, not I/O duration** (beat around blocking transport calls / beat from the transport layer), and the two budgets must be reconciled explicitly. **(2) The watchdog snapshot escapes its git-ignore:** `sdk/peer_run` derives `state_dir` from `log_path.parent`, so logging into a tracked directory writes `state_<role>.json` there, while `.gitignore` covers only `logs/` — an operational artifact (PRD_gatekeeper §7 D4) became committable. Fix: pin the snapshot to `logs/` (or ignore `state_*.json` repo-wide). **(3) The loud exit is silent on the console:** `os._exit(1)` skips stdout flushing — the stall exists only in the JSONL; flush before exiting. **(4) Replay's summary line is blank for every live game:** `peer/replay.replay_from_log` reads `event == "result"` but live peer logs emit `peer_result`, so `steps`/`outcome`/`game_uid` show `0`/`unknown`/`""` — the verdict itself is correctly computed (the M5 committed friendly re-hashes 26 turns/28 records, so that evidence claim stands), but the banner is a mandatory README screenshot. Rider: `verified = not problems` is **vacuously true when zero records are checked** — add a "≥1 record verified" guard so a truncated log cannot print "Verified OK". Also disclosed (interpretation, not a fix): an aborted game replays as **TAMPERED** because no records were ever revealed — fail-closed and correct for the book's binary §7.4 banner, but an honest crash is indistinguishable from tampering.

- ◐ **M7-8** 🚦 **At-least-once delivery: duplicate + reorder tolerance on the inbound path — the CODE half closed 2026-07-22; ☑ needs the live both-directions drill at the warm-ups** — C builds, review AG. **Raised by the Alon/Renat team (round 7), credited:** MCP-over-HTTP is at-least-once, so a push whose ack is lost is retried and ARRIVES TWICE; a receiver that calls a repeated step a protocol violation turns a flaky tunnel into a technical loss, and under App E **rule 35** contradictory/absent reports zero **both** teams — the threat is mutual, not competitive. **We were exposed on the live path** (and our own M7-7 push fix makes us a duplicate SENDER by design). Audit at `dfab03e` found three real gaps: (a) `peer/inbound` collapsed on any step ≠ `len(inbound)+1`, so a retry was a TECHNICAL_LOSS *and* a raised `ProtocolViolationError`; (b) no buffering — a step ahead of the awaited one collapsed identically; (c) `peer/p2p` renewed the turn deadline on ANY inbound message before judging it, so redelivered junk kept our clock alive on the opponent's behalf. **A fourth was found while fixing:** the deadline was only ever evaluated on an EMPTY poll, so continuous junk meant it was never evaluated at all. **Fix:** `peer/inbox_order.InboundSequencer` (pure, no game state) classifies every message BEFORE anything is applied — **duplicate** (commit already consumed or held) dropped with no second belief/scent pass and no deadline renewal; **buffered** (inside the `inbound_buffer_limit` window) held and replayed in order; **illegal** for a *different* commit at a played step (equivocation — the fraud the commit scheme exists to catch) or a step past the window (flood, and the window IS the flood rule — one threshold, not two). Dedup keys on the **commit**, not `(kind, step)`: unique per message, and the one thing a redelivery cannot vary. **The strict state machine stays strict** — illegal transitions still raise, malformed input still collapses pre-state-change, the M5 F10 caught-final-message exemption survives; the wall moved to the commit, it did not come down. Wire unchanged (constraint #13 untriggered): the ack still answers `status: "ok"` to an honest retry and carries a new `disposition`; a tolerated message logs `inbound_tolerated` so a drill can SHOW the duplicates arrived and changed nothing. Config: `[network] inbound_buffer_limit = 1` (private/unsigned, App B; game.toml → `version = "1.01"`), asserted `>= 1` as `reconcile_budgets` rule 5. `shared/config_model` split at its own signed/private seam → `shared/private_model` (150-line rule). DoD OBSERVED in permanent keyless CI: **six new chaos drills** (redelivery dropped · reorder buffered-and-replayed · equivocation still collapses · flood refused at the window · redelivery storm cannot renew the deadline · a full mini-game over a tunnel that duplicates EVERY push in both directions, mutual audit clean) + `tests/unit/peer/test_inbox_order.py`; battery now 19 drills; evidence `docs/evidence/m6-chaos.md` §M7-8. **LIVE HALF PAID 2026-07-22 (them → us):** a redelivery absorbed over the real Cloudflare edge against the live reference thief — the ack of its opening push dropped deterministically at our own edge by a lossy relay (racing a tunnel kill against a session teardown is a coin flip; the loss was induced by us, the RETRY is the reference's own code, `infra/mcp_client.py:42-55` checked first). `inbound_tolerated {duplicate, step 1}`, `turn_received` 15 vs 13 of our turns, **`belief` events 14 — one per ACCEPTED message, so nothing was applied twice**, game finished `cop_capture` 13 steps, mutual audit OK, replay **Verified OK** (29 records), exit 0. At `dfab03e` the same run was a technical loss. Evidence `docs/evidence/m6-chaos.md` §Live duplicate-delivery drill + `m7-8-dup-live-g1.jsonl`. **FINDING (read from the oracle, not drilled — `peer/turn_handler.py:41-48`): the reference has NO step-continuity check and applies a duplicate TWICE** (belief diffuse + smell observe + absorb + decay all re-run) — it fails silently where we failed loudly. Interop consequence: our own retried push can make a reference-based opponent decay its field twice, so **a `scent_physics_mismatch` we raise may be caused by a duplicate WE sent** — evidence-grade only (SQ3), never a verdict, but a named cause for any dispute write-up. **STILL OPEN for ☑: the us → them direction against a peer that DEDUPS** (the reference cannot demonstrate it, per the finding; Alon/Renat's client can) + the reorder half — both at the warm-ups, together with the wire-contract robustness line in the joint ADR (M7-0).

- ◐ **M7-4** 🚦 **Live-series artifact + the sealed sub-game index — found in the 2026-07-24 rehearsal with Alon/Renat** — C, review AG. Two halves. **(a) THE DEFECT (ours, rules 37-38 truth duty):** `sdk/peer_run` built the step-0 declaration without passing `sub_game_number`, so it fell back to the STATIC `[game] sub_game_number` in the TOML. `sdk/series_run` (self-play) always passed the real index; the LIVE rolling protocol — each sub-game its own process — never did, so **every sub-game sealed `sub_game_number: 1`**. Same class as the `num_games` find and worse in one respect: the index sits inside the **step-0 commit** from the first instant of the game, so a mis-declared sub-game can never be repaired in the report (index 1 seals `9237f54c…`, index 4 seals `2337737826…` — different commitments). Fixed with a `--sub-game N` seam threaded CLI → sdk → `peer_run` → seal; **omission still falls back to the config**, which is correct for a one-off game — the defect was never the fallback, it was the series never overriding it. **(b) THE ARTIFACT PATH:** a live tunnel series plays each sub-game in its own process, so by aggregation time every `PeerSession` is gone and `build_summary` has nothing to read. New `report/summary_from_log` rebuilds a reference-shaped summary from the COMMITTED log (our sealed records ride the `audit` event, their turns ride `turn_received`, the verdict rides `peer_result`) — a stronger property than reading live memory: **what we report is exactly what we archived**, re-derivable by a third party from the same bytes. New `report/series_from_logs` joins N logs to `emit_series`, producing the full counted-shaped set (6 configs + 6 logs + 6 reports + declaration + result), and **refuses the WHOLE series if any sub-game never settled** — a report that quietly drops a game is the contradictory report rule 35 punishes. Opponent identity is read from their archived `agreement_received`, with keys they never declared recorded **blank rather than invented** (a declaration block is a record of what a team stated). 708 keyless tests. **(c) THE OWNER — BUILT 2026-07-24, and it was the actual gap:** every piece above existed and *nothing fired them*. A live series plays each sub-game in its own process, so no process spanned the series and no code owned the moment it ended — a perfect 6/6 would have mailed nothing. New `sdk/live_series.run_live_series` plays the SIGNED `num_games` with roles alternating (F2, natural role on odd), joins the archived logs, and sends the ONE report the series owes; new `sdk/subgame_process` spawns each sub-game as its own `copthief run peer` child (the rolling-window shape, and a child this process spawns is a child it reaps — half the answer to the orphaned peer behind the phantom s6); new verb `copthief series`, with the parser split to `sdk/cli_args` for the 150-line rule. **`RunMode` is now PASSED** (`--rehearsal` / `--counted` on `run peer` and `series`, flags mutually exclusive, carried into every child because the child loads the config itself) — the M7-9 residual, discharged. `infra/email_sender.build_report_sender` is the single rail both the self-play and live series build (#11). **Committed `config/game.json` flipped `num_games` 1 → 6** (Imree's call, 2026-07-24): what we ship is the constitution we play the league under, byte-identical with the opponent's; the two tests that had pinned the shipped value as the App B single-sample default now inject it and pin the RULE. **DoD OBSERVED LIVE** — `docs/evidence/m7-4-live-series.md` + `m7-4-live-series-g1.jsonl` (replays **Verified OK, 37 records**) + the result artifact: two real `copthief series` processes on localhost, `cop_capture` 17 steps, mutual `audit_ok`, one shared `game_uid`, the **full artifact set on disk**, and the report rail REACHED FROM A LIVE GAME for the first time (interlock allowed it; the transport failed on a deliberately absent token, so nothing could leave — the send half is pinned keyless: one email per series, body bytes == artifact bytes, artifact attached). **Three defects the live run found, all fixed here:** a sub-game that left no log crashed the aggregation with a bare `FileNotFoundError` (now the same named refusal); a dead child left no trace of why (its stderr tail is in the record); a failed send swallowed the whole run record (now `{"action": "failed", …}` + CLI exit **3**, distinct from exit 2 = no report produced at all). 736 keyless tests, coverage 96.01%. **STILL OPEN for ☑: the completed 6-sub-game rehearsal itself**, which is blocked on M7-10 below, and one live send once the Gmail token is re-issued (OI-5, testing-mode refresh tokens last 7 days).
- ◐ **M7-10** 🚦 **The handshake identifies neither the pairing nor the role, and cannot survive redelivery — BUILT 2026-07-24; ☑ needs the opponent's half** — C builds, review AG; the index half is a MUTUAL fix agreed with the Alon/Renat team (their proposal, accepted). **BUILT, in three pieces, and the result is that a six-sub-game series COMPLETES for the first time** (before: the two sides desynchronised at sub-game 2 and never recovered). **(a) `peer/pairing` + handshake wiring** — `negotiate` now declares `sub_game_number` (the SEALED index, read from the step-0 record so the handshake and the commit cannot disagree) and `role`, and refuses a peer whose index differs or whose role EQUALS ours, judged before the `game_uid` is locked. Refusal truth table is the kit's (SPEC §7): a declared mismatch refuses, **omission never refuses**, and a value we cannot compare (wrong type, different spelling) is treated as silence rather than disagreement — refusing over a peer's type choice would turn a cosmetic wire difference into a forfeited game, and the rehearsal already lost a window to `payload` vs `message`. **Wire change, deliberate and safe (constraint #13):** two unsigned keys OUTSIDE `terms` — no canonicalization, hash or signature input moves, kit vectors untouched; same argument as M3-8's `scent_model_sha256`, which the reference ignores. **(b) handshake redelivery** — `exchange_agreement` re-pushes each lap instead of pushing once and waiting; classification unchanged (never delivered = `TransportError`, delivered but unanswered = `None`). **(c) `peer/port_guard`** — one live peer per role: refuse to start if something already holds our port (the starving peer consumes a sub-game and reports a timeout it did not cause), and refuse to play if OUR OWN server never came up (it runs on a daemon thread, so a failed bind raises where nobody is looking — exactly how the orphan's victim played on with a dead inbox). `run peer` answers both with a JSON refusal + exit 2, which the series driver records per sub-game. Also here: `PeerQueues.close()` — a settled peer refuses new arrivals on all four channels instead of swallowing the opponent's next greeting into a queue it will never drain, which turns their silent loss into an ordinary transport failure their existing retry resolves. **THE REPORT WAS REALLY SENT** (2026-07-24, Imree authorized the recipients): the six-sub-game series fired its own report through the live Gmail rail at series end — `{"action": "send"}`, recipients `imreeyal.copthief@gmail.com` + `imree.c@gmail.com`, subject `Police-Thief series result: winner imreeyal (reported by police)`, body and attachment both the 7095-byte `result_<game_id>.json`, exit 0 both sides. **The first email the project has ever sent**, and it closes the M7-6 residual: the 2026-07-20 send-only token works, the multi-recipient form a friendly needs works, and nothing human sat between the sixth sub-game settling and the mail leaving. **STILL OPEN for ☑: the opponent's half** — our refusal is inert until they declare the two keys too (staged: `notes/DRAFT-REPLY-ALON-R14.md`, which carries the exact field shape so the two sides cannot spell it differently). **PLUS M7-10b (preflight, ADR-0009 amendment): a report-owing run REFUSES TO START if it cannot deliver** — `EmailSender.preflight()` at the top of the series runs the interlock + a credential probe (`GmailTransport.verify_ready` refreshes the token, no send), so a disabled rail, an empty recipient, or a stale token is caught before a single sub-game plays rather than after six (rule 35). A `--rehearsal` can no longer run with mail disabled. Live: both refuse with zero logs, exit 2; the real token passes the probe. **UPDATE 2026-07-24 (ROUND 15): the Alon/Renat team CONFIRMED their half on the wire** — both keys top-level, our spelling and truth table, asserted on the emitted payload and every re-pushed copy, plus both mirror halves of the swallow; so the refusal is live in both directions and this drops to needing only the T + a diff of their (still unseen) new artifact.
- ☐ **M7-10 (superseded framing)** the finding as first recorded. Both halves were observed directly in the M7-4c live run (`docs/evidence/m7-4-live-series.md` §findings), not reasoned about. **(1) `negotiate` carries `{identity, nonce, scent_model_sha256, signature, terms}` — no sub-game index, no role.** Our sub-game 2 peer (thief) completed a handshake with a peer that was *also* thief, and the game deadlocked after one turn. The same hole produced the rehearsal's phantom sub-game 6: his s6 and our s2 were ONE game wearing two indices, undetectable from either side because identical terms give identical `game_uid`s. Fix: declare the sub-game index (and the role) in the handshake and REFUSE on mismatch — it catches the fault at the handshake instead of in the artifacts, and only bites if both sides send the fields, which is why it is being built mutually. **(2) A handshake can be swallowed by the opponent's PREVIOUS sub-game peer.** Under the rolling window the two sides finish milliseconds apart; the faster side's next peer pushes its agreement into the slower side's still-listening previous peer, which enqueues it and exits without draining. The slower side then waits out its whole connect budget for an agreement that was already delivered — and, having failed ~60 s faster than a real game takes, runs AHEAD of its opponent and never re-synchronises (observed: side B reached sub-game 4 while side A was still on 2). This is at-least-once delivery arriving at the **handshake**, where M7-8's tolerance does not reach; the answer is to keep re-pushing the agreement until the game starts — the same *"transport tolerance, no rules tolerance"* sentence the joint ADR already carries for turns. **Also owed and unbuilt: a single-live-peer-per-role guard** (two of our peer instances were alive at once during the rehearsal and nothing detected it — our half of the phantom-s6 find).
- ☑ **M7-11** **Per-role opponent dialing for the live series (the two-topology fix)** — C, review AG. Two series topologies exist in the league and both are real: the reference plays a whole series as ONE process at ONE address, while the Alon/Renat team serves TWO role-split services, each owning its own role's sub-games (their committed `league_series.py`: police repo plays the odd windows, thief repo the even ones, each repo's runner started at the T, next window launched on settle, the handshake re-push as the only cross-side barrier — read from their SHARED repo 2026-07-25, Imree-authorized). A driver that dials one URL for all six games is wrong half the time against the second shape. **Built (TDD, 3 red→green cycles):** `sdk/series_endpoints` — `SeriesEndpoints.for_opponent_role` maps THEIR role each sub-game to the URL we dial; `resolve_endpoints` accepts ONE address (both roles) or a split pair and refuses every ambiguous or incomplete combination; `endpoints_from_flags` lets the config's `opponent_url` apply only when no address flag was typed (a stale default must not refuse a correctly-typed split pair). `subgame_player` now dials `endpoints.for_opponent_role(opposite_role(role))`; `copthief series` gains `--opponent-police-url` / `--opponent-thief-url`. Wire untouched (dialing only, constraint #13 untriggered). **DoD OBSERVED in the M7-11 rig** (scratchpad `rig_m7_11.py`, quoted in the PR): our driver (`--role thief`, split pair, one process, port 8802) vs a faithful Alon-simulator (two independent sequential window-runners on 8811/8812, both started at T, 180s handshake patience per his config) — six windows settled strictly in order s1→s6 with the roles alternating, one shared `game_uid`, report rail refused at rest (dev mode), exit 0 — the shape whose 2026-07-25 predecessor rig deadlocked at sub-game 2. **Parity note for the T:** his police repo REFUSES even windows, so against his team we run `--role thief` (their police takes the odd sub-games; matches the rehearsal). Serving side: our one process needs BOTH our hostnames at its port for the run (`notes/FRIENDLY-RUNBOOK-2026-07-25/cloudflared-both-to-8802.yml`). **Disclosed residual (rig observation, not a friendly blocker):** in the EARLY-LAUNCHED position — a window started before its counterpart exists, which only the two-runner shape occupies, never our sequential driver (our side exited instantly all six times) — the LAST window consumed the opponent's audit exactly one turn-budget late (audit sat unread ~180s, then `audit_received` → settled `audit_ok true`, exit 0). Bounded, self-healing, both reports match; likely a stale handshake re-push sitting ahead of the audit in the inbox of a peer launched early. Diagnose if we ever build an early-launching runner of our own. **M7-11b (found by the FULL-DRESS rerun of the same rig — Imree's push: a friendly validation must include the mail, and running the complete format is what surfaced this): a bystander's agreement KILLED our handshake.** Against a role-split opponent, their next window's peer pushes early at our one port; its agreement carries the identical signed terms and a valid signature over its own nonce — it fails ONLY the M7-10 pairing check. `handle_negotiate` raised that refusal out of `run_peer_game`, the peer died, and every window where we moved second collapsed in cascade (dev-mode rig 1 escaped by winning the arrival race three times; the driver then honestly REFUSED to report — rule 35 behaving — exit 2, no mail). Fix: `PairingRefusal(NegotiationError)` raised only at the pairing check; `run_peer_game`'s handshake refuses a bystander ON THE RECORD (`agreement_refused` event) and keeps waiting for the real counterpart, bounded by the turn budget (the opponent's own declared negotiate bound); terms drift and bad signatures still raise on first offense. Pinned keyless: `tests/integration/test_handshake_bystanders.py` (bystander-then-counterpart plays to mutual audit; a bystander-only storm still ends in a loud bounded refusal; the gate itself still refuses a mispairing).
- ☑ **M7-18** **The evader reads capture claims — the discarded certainty (claim channel, half 1)** — C, review AG. PRD approved 2026-07-27 (#78), then built TDD (2 RED→GREEN cycles). **MEASURED, and the counted physics is where it lands: exact cop-tracking at decision time 0.311 → 0.936 under `multiplicative_book_v1` (121/389 → 364/389, 16 games, identical params both sides); under the shipped reference model 0.822 → 1.000** — small there because the age-voucher belief was already near-perfect (M3-3), which is exactly why both physics were probed. New `scripts/claim_probe.py` measures the PRODUCTION session logic (paired `PeerSession`s), not a referee model, with an in-memory `--scent-model` override so the counted physics is probed from the COMMITTED config dir (no local path in any artifact). **52.7% of the sender's turns carry no claim (STAY/BARRIER) and tracking still reaches 0.936** — a claim collapses certainty and the next silent turn's predict + honest scent usually holds the argmax. **⚠ FINDING THAT CORRECTS THE COMMITTED MODEL: M7-14 modeled claim-reading as `truth-lag1` ("one step late"); the peer path is lag 0** — the cop moves to X, claims X, and is STILL at X when our thief decides. New `config/arena_m7_18.json` isolates the channel (same brain, same options, only the feed differs) and the correction flips the conclusion: read at lag 1, claim-reading looked neutral-to-harmful vs our tuned cops (21/16 survivals vs 23/24 hidden); read at lag 0 it GAINS against every cop in the roster — ref-police 11→30, deployed generalist 23→27, and the lag-1 specialist loses most of its edge (16→23 survivals, its captures 16→9); aggregate thief points 770→800→**880**. **FOLLOW-UP NAMED, NOT ACTED ON: the cop's book-v1 GA pool should use the lag-0 arm, not `truth-lag1` — `police-brain-lag1spec` was tuned against a weaker opponent than exists. No cop weights changed in this milestone.** Regression posture: `m5-arena.md` + `m7-14-bookv1-arena.md` + `m7-14-reference-arena.md` all regenerate BYTE-IDENTICAL (the referee path reaches belief through feeds, never through `peer/inbound.py`). **Deployed-thief number is OWED to the sibling repo** (this repo's `thief_class = "random"`; the M7-16 5/16 baseline is re-measured there after the sync). Evidence: `docs/evidence/m7-18-claim-channel.md` + generated `m7-18-claim-tracking.md` / `m7-18-claim-channel-arena.md`. Original finding: Verified gap: `peer/inbound.py:84-89` answers the opponent cop's `capture_claim` honestly and **discards its position content** — `session.belief`, which is precisely a distribution over where the opponent is, never sees it. A claim is truthful by rule (App E 21-22, PDF p.145: false claims cost the game with no appeal) and is a plaintext pre-reveal of a cell already sealed in the same message's `commit`, so it is certainty-grade evidence in the same class as a declared barrier — which the filter already treats as certain (`note_barrier` eliminates mass). **Sharper than the M7-13 postmortem recorded (re-derived from the tree, PRD §3.3): the opponent's start is signed constitution and claim ABSENCE is equally informative under the reference policy (a barrier turn leaves `session.position` unchanged, `peer/turns.py:64-71`), so a claim-reading thief facing a reference-policy cop has the cop's exact cell on every turn — full observability of the pursuer, not merely a strong observation.** Build: `BeliefFilter.note_claim` (hard collapse, Imree's decision 1) + one call site after `predict()`, before `update_scent()`; **mirrored core, so role-blind tests (gotcha #9)**. Absence-reading deliberately NOT in half 1 — it is sound only against an unconditional claimer, so it is scoped as a config-gated arena arm, default off. Measured on the committed M7-14/15/16 configs (`truth-lag1` already models unconditional claiming); the M7-16 5/16 survival vs the belief-chaser under book-v1 is the gap to close, reported honestly whichever way it goes. DoD: PRD approved; §8.1-6 green; thief pytest green BEFORE the sync commit; evidence `docs/evidence/m7-18-*.md` regenerates deterministically; kit CORE untouched (payload content, not shape — constraint #13 not triggered, stated in the PR body).
- ☑ **M7-20** **Retuning the cop under the claim policy — a NULL RESULT, honestly reported (claim-channel follow-up)** — C, review AG. Closes the follow-up M7-18/M7-19 opened: the cop's GA pool used a claim-reader model M7-18 proved wrong (unconditional `truth-lag1`, when the real channel is LAG 0 and, under M7-19's policy, only arrives on turns we declared), and the cop had never evolved under its own claim threshold. **Build:** GA knobs `claim_threshold` (run-level — a police property) and `claim_feed` (per pool member with a run-level fallback, because the pool's point is a MIXTURE); `genetic/runs.py` crossed 150 source lines and was SPLIT into `genetic/fitness.py` (never compressed); `_fitness_vs` promoted to the tested public `opponent_fitness`. **Inertness proven: `ga_bookv1.json` regenerates `ga_weights_bookv1.json` BYTE-IDENTICAL (same md5) with the same 0.641→0.688 curve**, so no committed GA artifact is disturbed. **VERDICT: the retuned vector LOSES the champion gate in BOTH physics — book-v1 1700 vs the champion's 1760 (losing EVERY pairing) and reference 2420 vs 2900 — so per CLAUDE.md §5 it is NOT DEPLOYED.** Weights committed as a labelled negative-result artifact (`config/ga_weights_bookv1_claims.json`). **Two measured causes, both reusable:** (a) the first attempt at 16 seeds produced a COMPLETELY FLAT curve — default/deployed/evolved all exactly 0.6406 despite `w_distance` 0.66 vs 6.0 — an instrument-resolution artifact, not an empty landscape; **before believing a flat GA curve, measure per-member spread across deliberately extreme vectors**; (b) **the claim policy removes the selection pressure it was meant to add** — the claim-reader member's spread collapses 0.094→0.031 once we go quiet, so blinding the opponent blinds our own tuner. **BIGGER FINDING, re-confirmed and now sharper: under the COUNTED physics the plain `ref-police` heuristic tops the table (2075 vs our champion's 1760) — strong against four arms, collapsing only against the claim-reader (2/32, it always declares) — while our tuned vector is MEDIOCRE EVERYWHERE (9–10 captures vs every arm). Our GA may be optimising toward flat mediocrity, and the counted series runs on exactly that physics.** Also fixed: `scripts/ga_run.py` hardcoded `config/ga_weights.json` into every generated evidence doc regardless of `artifact_out`, so the committed M7-14 doc pointed readers at the wrong file (doc↔repo gap); now derived, and that doc regenerated. Evidence: `docs/evidence/m7-20-ga-under-the-claim-policy.md` + generated `m7-20-ga-claims.md` / `m7-20-gate-bookv1.md` / `m7-20-gate-reference.md`. **STILL OPEN: (1) why `ref-police` beats our vector under book-v1 — highest-value question before the counted games; (2) the pool's dead weight (`random` discriminates 0.000); (3) the thief-side retune.**
- ☑ **M7-19** **The quiet cop: claim-gated referee physics and the points sweep (claim channel, half 2)** — C, review AG. **Gated on the same PRD approval, and on M7-18 landing first.** Our cop claims its landing cell on every moving turn (`peer/turns.py:117-123`, mirroring reference `turn_sender.py:45-48`) — a free position feed to any claim-reading evader, measured in M7-14 as roughly halving the plain chaser's capture rate (18→5 of 32). **Legality re-derived from primary source for the PRD, not inherited: the book mandates the RESPONSE duty (p.38 iron rules — a cryptographic duty on the THIEF to answer a declared claim) and forbids FALSE claims (App E 21-22); it never mandates claim FREQUENCY. The argument is by contrast, not from silence — one sentence later in the SAME p.38 paragraph the book mandates that the cop declare EVERY barrier placement (`כל`) and forbids placing one in secret (`בהיחבא`); it wrote both halves for barriers and only the truthfulness half for claims. Scoring table 2 (p.38) prices silence honestly: a successful capture is the cop landing on the thief's cell AND declaring — so a quiet cop forfeits that landing capture. Self-punishing, not illegal. The barrier and imprisonment capture forms are NOT claim-gated, so a quiet cop keeps its whole trap game — which matters, our deployed book-v1 vector being herding-shaped (`w_budget = 0`).** Build: referee claim modeling (same-cell endings gated on the cop's own last-turn claim, BOTH half-turns — the friendly's g06 capture rode the cop's standing claim and **our belief was wrong at that moment, so a positive threshold forfeits it; that case is a passing regression test, not a caveat**) + claim-conditional thief information (composed at the loop level — truth delta on claimed turns, hidden scent otherwise; no `BeliefFeed` Protocol change) + per-police-roster `claim_threshold`, default 0.0 so every committed arena table reproduces exactly. **Sweep objective = expected SERIES POINTS under `config/game.json` (20/5/10/5), NOT capture rate, across an opponent MIXTURE (claim-reader AND trap-naive sitter AND ref-thief) via the M7-15 `opponent_pool` machinery — a threshold tuned only against the announced counter donates points everywhere else. Reports PER-OPPONENT-TYPE optima: the threshold is private unsigned config and counted series are one per opponent, so deployment is per-match like the M7-15 overlays. Default posture = always claim; the burden of proof is on going quiet.** Two extra deliverables: (a) a claim-reaction read in the postmortem tooling (does an opponent's logged thief correlate with our claimed cells? the friendly baseline says his current thief ignores them — this is what decides whether quiet is worth anything against a given opponent, and it gates M7-18's absence-reading arm); (b) OPTIONAL, cost/benefit first — an in-series adaptive policy mirroring the M5-5 hint profiler; **if the static per-opponent threshold captures most of the value, prefer it and record why**. **Wire legality of a claim-less MOVE turn: VERIFIED 2026-07-27 against the oracle's SOURCE, and the answer is structural** — `capture_claim` has a `None` default so it is never in `from_dict`'s required set; `turn_handler.py:57` guards `if message.capture_claim:` with no else and no validation; and decisively **the true move is not on the wire at all** (`domain/protocol.py` docstring: position/move/verdict are sealed in `commit`, proven only at audit), so a receiver *cannot* condition on "a MOVE turn must carry a claim" — the check is impossible, not merely absent. Supersedes the planned live localhost oracle game, which was scoped to discover this and cannot even run without the gated policy it was meant to gate; **demoted to a free confirmation riding the half-2 build** (one local game, threshold high enough to silence the cop, clean audits + Verified-OK replay). Real residual = the opponent's independent client, discharged at the friendly both halves must meet before any counted game. **BUILT + SWEPT (`docs/evidence/m7-19-quiet-cop.md`, generated `m7-19-claim-sweep.md`):** `check_end(claim_standing=...)` gates ONLY the landing form; `strategy/referee_claims.ClaimPolicy` owns the decision; the referee gates BOTH half-turns on the cop's own last claim and **the g06 case is a PASSING regression test, not a caveat**. `claim_threshold` is per police roster entry and `claim_feed` per thief entry (the sweep needs a MIXTURE where some opponents read claims and others do not). **Sweep result (32 seeds, book-v1, cop points/game): a threshold of 0.1 is BEST-OR-TIED on every opponent type — +1.88 points/game vs a claim-reader (7.81 → 9.69, +24% over what we ship) and exactly zero cost against claim-blind / camper / ref-thief.** The flat columns were checked, not assumed: **our deployed vector has ZERO landing-form captures** (loud == silent against all three non-reading arms), so the claim channel is pure downside for it — it wins by walling, which the book never lets a cop withhold. **The contrast case is COMMITTED in the same instrument: a plain chasing cop goes 32/32 → 0/32 when silenced, so a claim policy can never be deployed independently of the weight vector it runs under.** Regression: all four committed arenas regenerate byte-identical (threshold absent ⇒ unmodelled). `simulation.py` crossed 150 lines and was SPLIT into `sdk/simulation_referee.RefereeSeriesMixin`, never compressed. **WIRED INTO THE LIVE EMITTER AND DEPLOYED:** `PeerSession.claim_policy` reads `claim_threshold` from `[strategy.<role>]` with the M7-15 per-model overlay (absent = 0.0 = the historical emitter), and `game.toml` v1.02 sets **0.1 on the book-v1 police overlay only** — inert under the shipped default model, exactly the M7-15/16 posture, because book-v1 is the only physics the sweep measured; merging IS the deployment decision. **Live peer-path validated:** overlay resolves (0.1 book-v1 / absent default); a claim-reading opponent's tracking of our cop falls **0.936 → 0.433** (the mirror of half 1, same instrument); full local mini-game = mutual audits OK both sides + replay Verified OK (71 records), so the policy is wire-silent. **DEPLOYMENT POSTURE (stronger than the PRD asked): the PRD wanted per-opponent optima; the measurement says ONE standing number is the answer — 0.1 is best-or-tied against readers AND non-readers, so it needs NO intel about which team we draw, which is what a multi-team league requires.** **Recommendation on PRD §5.4b (Imree's open decision 2): do NOT build the in-series adaptive policy** — a dominant static threshold leaves it nothing to discover, and it would add live-path behaviour for no measured gain. Follow-up: the GA pool should retune against the lag-0 reader AND with the claim policy in the loop.
- ☑ **M7-22** **The derived `game_uid` is declared at negotiate — uid divergence refuses at T instead of surfacing at the report diff** — C, review AG; the mutual closure of the opponent team's 2026-07-25 uid bug (their deterministic derivation hashed the WRONG input — their whole `game.json` — and stayed internally self-consistent through an entire series because the uid never crosses the wire; nothing compares the two sides' derivations until the reports meet). Built in the M7-10 pattern, kit §7 truth table unchanged: a peer that knows its opponent a priori (`--opponent-group`, which every series child now receives) declares `game_uid(flat terms, sorted group ids)` top-level in the greeting, OUTSIDE `terms` (no signature input moves); the receiver refuses a COMPARABLE mismatch naming the diagnosis ("check the terms input"); **omission never refuses** (the reference declares nothing) and an uncomparable value is silence. Judged after the pairing check, so only the true counterpart ever reaches it. Pinned in `tests/unit/peer/test_uid_declaration.py` (declare / omit / match / refuse / omission-tolerance / uncomparable-silence) + the child-argv pin. Kit half: PROPOSED registration via the kit session; the opponent team is invited to build theirs (draft reply); the cross-team warm-up is the promotion path.
- ☑ **M7-17** **`game_id` is the SORTED pair — the friendly's report-diff cosmetic mismatch, closed at the root** — C, review AG. The 2026-07-26 cross-team report diff found each side naming the match self-first (`imreeyal-vs-anrbj666` vs `anrbj666-vs-imreeyal`). Verified against the oracle: the reference **derives** it — `domain/game_ids.py` `pair = sorted([group_a, group_b])` — exactly like the pair term inside `game_uid`, so both peers produce one string with no convention to settle; the kit pinned it (SPEC §4, swapped-order vector, PR #11) and the opponent's naming already matched the sorted form. New pure `domain/crypto.series_game_id` (beside `game_uid`, mirroring the reference's pairing); all three construction sites now derive through it (`sdk/live_series`, `report/series_from_logs`, `sdk/series_run`); the integration harness derives rather than re-encodes. Historical artifacts (the committed friendly set) stay as played — evidence is never renamed.
- ☑ **M7-12** **The refusal names absence, not just disagreement** — C, review AG. T3 of the 2026-07-25 friendly burned a window and two diagnosis-hours on `terms mismatch: opponent terms do not value-equal ours` when the inbound agreement carried NO `terms` key at all — the opponent's bookletter-shaped greeting (`config_sha256` substitution) under a reference wire, a shape the 07-24 rehearsal had already met once. `handle_negotiate` now distinguishes: a terms-less agreement refuses with `opponent agreement carries no terms at all — a bookletter-shaped greeting under a reference wire (kit CORE: flat terms + nonce + signature)`; value-unequal terms keep the mismatch wording. Pinned both ways in `tests/unit/peer/test_handshake_pairing.py`.
- ☑ **M7-15** **The generalist retune: mixed-opponent GA fitness, per-model weight deployment, and the four-cop gate (counted-series prep, closure half)** — C, review AG. Closes the two M7-14 follow-ups. **(a) `GaConfig.opponent_pool`** — fitness = plain mean across `{spec, feed?, options?}` members, pinned exactly against the single-opponent runs it is built from; `config/ga_bookv1.json` now tunes against the four measured worlds (claim-reader / announced-fix / ref-thief / random). Pool fitness 0.641→0.688; the evolved vector is a chase-heavy GENERALIST (w_distance at the box ceiling, w_budget restored) — the opposite shape of the M7-14 lag-1 specialist. **(b) The four-cop, five-thief gate, both physics (`m7-15-mixed-retune.md`):** the pool vector does NOT lose to the previous champion in either physics (book-v1 1790 vs 1610; reference 3080 vs 3050) — the CLAUDE.md §5 rule holds without a physics caveat; the lag-1 specialist keeps the best single-arm number vs the ANNOUNCED counter (16/32) but pays under reference (2705), kept committed as the revisit-on-intel option; the M7-14 passivity anecdote does not reproduce in referee aggregate (26-27/32 vs random). **(c) `[strategy.<role>.<scent_model>]` overlays** (`PrivateSettings.strategy_options`, used by `PeerSession`): a sub-table named after a scent model applies only when that model is selected — DEPLOYED: the pool vector rides the book-v1 overlay in `game.toml`, the base table keeps the proven reference vector (the base-swap alternative is the config reviewer's to take; merge = the deployment decision). Live-validated on the peer path: overlay resolves, full local mini-game clean audits + Verified-OK replay, shipped default untouched. **Still open, in order: the sibling thief retune (rides the next sync) and the claim policy (needs the claim channel modeled in the referee first — a naive threshold would have missed the g06 collision capture).** DoD: regenerates deterministically from committed configs; 827+ keyless tests.
- ☑ **M7-14** **Strategy under the counted physics: model doors, the trap-aware evader, the belief fix, and the book-v1 retune (counted-series prep, build half)** — C, review AG. Analysis layer: `docs/evidence/m7-14-bookv1-strategy.md`; generated instruments `m7-14-bookv1-arena.md` + `m7-14-reference-arena.md` + `m7-14-ga-bookv1.md` + regenerated `m3-belief-eval.md`. **(a) Doors:** the referee/arena/GA path had NO scent-model door — every deployed GA weight was tuned under `subtractive_chebyshev_v1` only. Now `shared/locked_models.build_scent_model` (one builder, shared with the peer session) feeds `play_referee_game`/scenario series/`config/arena*.json`/`config/ga*.json`; per-side information feeds (`make_feed`: `hidden`|`truth`|`truth-lag<K>`, fresh per game) model the M7-13 claim-reading finding as thief-side lag-1 truth. **(b) The opponent model:** `strategy/evader_brains.BeliefEvaderBrain` in the postmortem's priority order (belief-native flight Manhattan+Chebyshev · anti-camping under a Chebyshev threat radius · wall forecast via mobility + `strategy/region`), ablation arms config-expressed (`evader-nofix`/`evader-fix`/`evader-lag1`). **(c) The belief fix — argmax 17% → 61%:** under the book model the filter now shape-matches the observation INNOVATION (observed minus decay-predicted previous field = one fresh kernel at the current cell, by the model's own arithmetic; `domain/belief_observation`), so saturated camp blobs read as mismatch, not youth; the reference voucher path is byte-identical (0.977/0.7314 stand). Raw-shape matching alone was tried and measured WORSE (0.086) — the innovation is the fix. **(d) Measured (32 seeds, deterministic):** the arena reproduces the opponent team's own published number (27/32 lag-1 captures, reference physics) — instrument cross-validated; the reference-tuned weights UNDERPERFORM the plain chaser under book-v1 (27 vs 63 of 128 — the measured cost of tuning under the wrong physics); his announced trap-fix WITHOUT better information is captured MORE (the trap-confound, measured from the evader side); claim-reading is the counter that matters (chaser 18→5) and the cheapest defense is ours to take (belief-thresholded claim policy — named follow-up, NOT built). **(e) Retune through the gate:** GA under book-v1 vs `evader-lag1` (fitness 0.406→0.531, seeds 401-432 disjoint), evolved vector's headline `w_budget = 0` (herding beats chasing an informed evader); three-cop gate comparison BOTH physics: new beats old champion under book-v1 (1270 vs 1045 pts), old keeps reference (2425 vs 2110) — **the two vectors are physics-specific, both committed (`config/ga_weights_bookv1.json`), NOTHING deployed** (game.toml untouched; per-model `[strategy.police.<model>]` selection = candidate follow-up, Imree's call). **Sibling thief retune rides the core sync after merge** (its camping streaks to 14 are unpriced risk); disclosed limits: GA specialized vs lag-1 (pays vs naive arms), evader model brackets announced behavior rather than predicting it. DoD: all four instruments regenerate deterministically from committed configs; CI arena + champion pin untouched and green; 822 keyless tests.
- ☑ **M7-13** **Capture postmortem: how the g02/g06 cops actually won (counted-series prep, analysis half)** — C, review AG. The two friendly captures re-derived step-by-step from the committed logs (`docs/evidence/m7-13-capture-postmortem.md`): our cop's positions from our own audit, their thief's from their audited move list in `audit_received`, cross-checked against the claim/response chain and the capture cells. **The mechanism is opponent-position modeling, not trap-dodging:** g02 = ten terminal STAYs at `(5,1)` with all four exits open — a camper is a self-refreshing beacon under `multiplicative_book_v1`, and our argmax locked on after seven turns of pumping; g06 = the thief walked into the pursuit lane while our argmax still pointed at its abandoned campsite; g04 (the contrast survival) = a thief that kept relocating and was saved by the 35-step clock with our cop at Chebyshev 1. Cross-cutting: live argmax hit-rates 5/32, 1/35, 1/12 confirm the M3-8 saturation finding but error distance stayed ≤ 2 (the cop pursues mass, not peak); our own thief camps HARDER (streaks to 14) and survived only because their cop never exploited the beacon; the hint channel contributed nothing (their phrase-book parsed to `motion_prior {None: 1.0}`); and **our claim-per-step habit hands any claim-reading evader our cop's position for free — likely their cheapest counter and ours to stop volunteering.** Feeds M7-14 (the arena's trap-aware evader must get cop-tracking + claim-reading FIRST, anti-camping second, wall-forecast third) and the sibling retune (our camping habit is unpriced risk once their cop learns to convert beacons). DoD: evidence doc committed; every number re-derivable from the committed logs by the quoted script.
- ☑ **M7-9** **Split the `counted` flag: App F counted semantics WITHOUT making the lecturer addressable — BUILT 2026-07-24 (ADR-0009)** — C, approve I. Today `counted=True` does two jobs: it arms the App F fixed rows (forcing `num_games=6`, a real series) AND it is what makes the lecturer addressable at all (below it the interlock refuses him mechanically). A dress rehearsal wants the first without the second. Imree's framing (2026-07-24): a friendly should differ from a counted game **only** in that it is not counted and the lecturer is not on it — like a football friendly, full ninety minutes, same rules. Splitting them makes the lecturer **structurally unreachable during a rehearsal** rather than merely unconfigured (Alon: *"safety by shape, not by configuration"*). Gate: `docs/adr/0009-rehearsal-mode.md` (amends ADR-0008's MECHANISM, not its posture). **Built:** `shared/run_mode.RunMode` carries `strict_rules` (arm the App F rows) and `counted_series` (scores league points); `RunMode.rehearsal()` = rules armed + lecturer unreachable, `RunMode.counted()` = both. **The lecturer needs BOTH, enforced by construction** — `counted_series` without `strict_rules` raises, so the split never makes him easier to reach than ADR-0008 left him. The email layer now takes `lecturer_addressable` rather than `counted` (`decide_email_action`, `EmailSender`), because handing that layer the RULES flag was the confusion being removed; both default closed. `SimulationSdk(counted=True)` survives as the legacy spelling of axis 1 only and can no longer imply the lecturer is reachable. **Uncomfortable fact this recorded:** because `counted=True` was unsafe to pass, NOTHING ever passed it — every live game, including the 2026-07-24 rehearsal, ran with the App F rows **disarmed**; the constitution was counted-shaped only because it was set by hand. Rules were followed, not enforced. 714 keyless tests. **RESIDUAL DISCHARGED 2026-07-24 (M7-4c):** `--rehearsal` / `--counted` are now real CLI flags on `run peer` and `series`, mutually exclusive, mapped by `sdk/cli_args.run_mode_from_args` and carried into every sub-game child (the child loads the config tree itself, so a driver that armed the rows only in its own process would have relocated the defect rather than fixed it). First live game played under the ARMED rulebook: `docs/evidence/m7-4-live-series.md`.

## Phase M8 — Submission hardening

- ☐ **M8-1** README academic reports ×2 (§9.4.2 sections + user-manual sections + contradiction-choices narrative + screenshots + sibling links) — C, review E+AG, approve I. DoD: `check_submission.py` README items green.
- ☐ **M8-2** KNOWN_LIMITATIONS.md + SELF_GRADE.md (`self_grade.py` output; code-quality only) — C, approve I.
- ☐ **M8-3** Full checklist sweep (`check_submission.py` fully green both repos; guidelines §17 + App C table 6) — C+I.
- ☐ **M8-4** Annotated tags `v1.0-submission` pushed both repos; Moodle per-member submission + PDF form + group ID (OI-1 resolved) — I.
- ☐ **M8-5** Final doc↔repo alignment audit (README numbers vs tree; PROMPTS.md truthful/complete) — C+I. DoD: zero contradictions found.

## Standing (every phase)

- ☐ **S-1** PROMPTS.md per PR (committed work only) — C.
- ☐ **S-2** Conventional commits; branch→PR→AG review→squash; never push main — all.
- ☐ **S-3** TODO statuses updated as work lands — C.
- ☐ **S-4** Risk register reviewed at each milestone exit (PRD §10) — I+C.
- ☐ **S-5** No email is ever sent to an address I has not configured for that run, and **never to the lecturer** without his explicit word — I (mechanical: the interlock refuses an empty recipient list, and refuses the configured `lecturer` address whenever `counted` is false). **Reworded 2026-07-20 by ADR-0008** — the previous "no email leaves draft without a per-send word" was superseded when draft stopped being a posture (App E rules 30/32/34/35 + §9.3); missed in the M7-6 build's doc sweep and corrected here. Outreach mail (league coordination, team correspondence) keeps the original per-send rule — see the parent-workspace `CLAUDE.md`.
